import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F22771",
    },
    secondary: {
      main: "64BAAA",
    },
    warning: {
      main: "FF7171",
    },
  },
});

export default theme;
